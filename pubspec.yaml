name: kasir_services
description: Pure Dart abstractions for receipt printing and local backups.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=2.19.0 <4.0.0"

dev_dependencies:
  test: ^1.24.0
